_schema-version: 3.3.0
ID: enterprise-ai-assistant
description: A simple CAP project.
version: 1.0.0
modules:
- name: enterprise-ai-assistant-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: enterprise-ai-assistant-auth
  - name: enterprise-ai-assistant-db
  - name: enterprise-ai-assistant-destination
  - name: mcp-api                            # ← injected MCP_SERVER_URL here
    properties:
      MCP_SERVER_URL: ~{mcp-url}
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: enterprise-ai-assistant-mcp
  type: nodejs
  path: mcp
  requires:
  - name: enterprise-ai-assistant-auth
  - name: enterprise-ai-assistant-db
  provides:
  - name: mcp-api
    properties:
      mcp-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
    memory: 256M
    disk-quota: 256M
  build-parameters:
    builder: npm-ci
- name: enterprise-ai-assistant-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: enterprise-ai-assistant-db
  parameters:
    buildpack: nodejs_buildpack
- name: enterprise-ai-assistant
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: mcp-api                            # ← expose MCP to Approuter
    group: destinations
    properties:
      forwardAuthToken: true
      name: mcp-api
      url: ~{mcp-url}

  - name: enterprise-ai-assistant-auth
  - name: enterprise-ai-assistant-destination
  - name: enterprise-ai-assistant-html5-runtime
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: enterprise-ai-assistant-app-deployer
  type: com.sap.application.content
  path: gen
  requires:
  - name: enterprise-ai-assistant-auth
  - name: srv-api
  - name: enterprise-ai-assistant-html5-repo-host
    parameters:
      config:
        HTML5Runtime_enabled: true
      content-target: true
  build-parameters:
    build-result: app/
    requires:
    - artifacts:
      - enterpriseaipurchaseorders.zip
      name: enterpriseaipurchaseorders
      target-path: app/
- name: enterpriseaipurchaseorders
  type: html5
  path: app/purchaseorders
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: enterprise-ai-assistant-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
        redirect-uris:
        - https://*~{app-api/app-uri}/**
      tenant-mode: dedicated
      xsappname: enterprise-ai-assistant-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
  requires:
  - name: app-api
- name: enterprise-ai-assistant-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: enterprise-ai-assistant-destination
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
    service: destination
    service-plan: lite
- name: enterprise-ai-assistant-html5-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-host
- name: enterprise-ai-assistant-html5-runtime
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-plan: app-runtime
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
